# Test Layerset Build

E2E test repo for `layer-and-layerset-build` workflow (layerset type) in buildon-github-actions.

Tests layerset type with range resolution, dependency validation, and metadata injection. References test-layer-build (same org) and a real layer from kube-kaptain org (cross-org) to exercise explicit long-form image URI resolution.
